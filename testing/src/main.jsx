import React from "react";
import ReactDOM from "react-dom/client";
import Router from "./Router"
import "./styles/Globle.css"

ReactDOM.createRoot(document.getElementById("Testing")).render(
  <React.StrictMode>
    <Router />
  </React.StrictMode>
);
